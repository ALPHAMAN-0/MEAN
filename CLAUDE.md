# CLAUDE.md

- No manifest present (no package.json, go.mod, etc.) — no build/test/lint commands exist yet.
- Repo currently contains only `README.md` and `.gitignore`; there is no source code to edit.
- Do not invent a `server/`/`client/` scaffold — only create it if explicitly asked; README's setup steps (lines 51-62) are suggestions, not existing structure.
- Files worth reading first:
  - `README.md` — full description of the intended MEAN stack project
  - `.gitignore`
  - `ARCHITECTURE.md`

Architecture: see ARCHITECTURE.md — read before structural changes
